# 🎮 Aether UI 文档站点

## 概述

这是 Proton UI 的官方文档站点，基于 Rspress v1.47.1 构建，提供完整的组件文档、使用指南和 API 参考。

## 🚀 快速开始

### 开发模式
```bash
pnpm dev --filter=@proton-ui/docs
```

访问 http://localhost:3000

### 构建生产版本
```bash
pnpm build --filter=@proton-ui/docs
```

### 预览构建结果
```bash
pnpm preview --filter=@proton-ui/docs
```

## 📁 目录结构

```
packages/docs/
├── docs/                    # 文档内容
│   ├── index.mdx           # 首页
│   ├── guide/              # 指南文档
│   │   ├── getting-started.mdx
│   │   ├── architecture.mdx
│   │   └── streaming.mdx
│   ├── components/         # 组件文档
│   │   ├── button.mdx
│   │   ├── input.mdx
│   │   ├── card.mdx
│   │   ├── message.mdx
│   │   ├── chat-container.mdx
│   │   └── streaming-text.mdx
│   └── public/             # 静态资源
│       ├── logo.png
│       └── custom.css      # 自定义样式
├── rspress.config.ts       # Rspress 配置
├── package.json
└── tsconfig.json
```

## 🎨 自定义样式

文档站点使用自定义 CSS 实现像素风格的视觉效果：

- **主色调**: #FF6B9D (像素粉)
- **次色调**: #4ECDC4 (像素青)
- **强调色**: #FFE66D (像素黄)

自定义样式文件位于 `docs/public/custom.css`，包含：
- 像素风格滚动条
- 悬停效果优化
- 卡片样式增强
- 代码块样式优化

## 📝 编写文档

### 添加新页面

1. 在 `docs/` 目录下创建 `.mdx` 文件
2. 在 `rspress.config.ts` 中添加导航配置

### MDX 语法

支持标准 Markdown 语法和 React 组件：

```mdx
# 标题

这是一段文本。

## 代码示例

\`\`\`tsx
import { Button } from '@proton-ui/core';

function App() {
  return <Button>点击我</Button>;
}
\`\`\`

## 组件演示

<Button variant="primary">示例按钮</Button>
```

## 🔧 配置说明

### rspress.config.ts

主要配置项：

- `root`: 文档根目录
- `title`: 站点标题
- `description`: 站点描述
- `themeConfig`: 主题配置
  - `nav`: 顶部导航
  - `sidebar`: 侧边栏
  - `socialLinks`: 社交链接
  - `darkMode`: 暗色模式支持
- `plugins`: 插件配置
- `builderConfig`: 构建配置

## 🎯 特性

- ✅ 响应式设计
- ✅ 暗色模式支持
- ✅ 代码高亮
- ✅ 组件预览
- ✅ 搜索功能
- ✅ 自定义样式
- ✅ Emoji 支持 🎮

## 📚 文档内容

### 指南
- 快速开始
- 架构设计
- 流式渲染原理

### 组件
- Button 按钮
- Input 输入框
- Card 卡片
- Message 消息
- ChatContainer 对话容器
- StreamingText 流式文本

## 🐛 问题排查

### 页面显示空白
- 检查 `docs/index.mdx` 是否存在
- 确认 `rspress.config.ts` 配置正确
- 清除缓存：`rm -rf node_modules/.rspress`

### 样式不生效
- 确认 `custom.css` 在 `docs/public/` 目录下
- 检查 `rspress.config.ts` 中的 HTML tags 配置
- 清除浏览器缓存

### 构建失败
- 检查 MDX 语法是否正确
- 确认所有依赖已安装
- 查看错误日志定位问题

## 📖 参考资源

- [Rspress 官方文档](https://rspress.dev/)
- [MDX 语法](https://mdxjs.com/)
- [React 文档](https://react.dev/)

## 🤝 贡献

欢迎贡献文档内容！请遵循以下步骤：

1. Fork 项目
2. 创建特性分支
3. 提交更改
4. 推送到分支
5. 创建 Pull Request

---

**Made with 💖 by Proton UI Team**
