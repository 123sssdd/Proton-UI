# 文档组件测试

本页面用于测试所有文档组件是否可以在 MDX 中正常使用。

## DemoBox 组件测试

### 基础用法

<DemoBox
  title="按钮演示"
  description="这是一个简单的按钮演示"
  code={`import { Button } from '@proton-ui/core';

function App() {
return <Button>点击我</Button>;
}`}
language="tsx"

>

  <button className="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600">
    点击我
  </button>
</DemoBox>

### 带属性面板的演示

<DemoBox
  title="可配置按钮"
  description="通过属性面板调整按钮样式"
  showProps={true}
  propsConfig={[
    {
      name: "variant",
      type: "select",
      options: ["primary", "secondary", "outline"],
      defaultValue: "primary",
      label: "变体",
    },
    {
      name: "size",
      type: "select",
      options: ["small", "medium", "large"],
      defaultValue: "medium",
      label: "尺寸",
    },
    { name: "disabled", type: "boolean", defaultValue: false, label: "禁用" },
  ]}
  code={`<Button variant="primary" size="medium">按钮</Button>`}
>
  <button className="px-4 py-2 bg-blue-500 text-white rounded">按钮</button>
</DemoBox>

## PropsTable 组件测试

<PropsTable
  data={[
    {
      name: "variant",
      type: "'primary' | 'secondary' | 'outline'",
      required: false,
      defaultValue: "'primary'",
      description: "按钮变体样式",
    },
    {
      name: "size",
      type: "'small' | 'medium' | 'large'",
      required: false,
      defaultValue: "'medium'",
      description: "按钮尺寸",
    },
    {
      name: "disabled",
      type: "boolean",
      required: false,
      defaultValue: "false",
      description: "是否禁用按钮",
    },
    {
      name: "onClick",
      type: "() => void",
      required: true,
      description: "点击事件处理函数",
    },
  ]}
/>

## CodeBlock 组件测试

### 基础代码块

<CodeBlock
  code={`function greet(name: string) {
  console.log(\`Hello, \${name}!\`);
}

greet('World');`}
language="typescript"
/>

### 带文件名的代码块

<CodeBlock
  filename="example.ts"
  code={`export const config = {
  apiUrl: 'https://api.example.com',
  timeout: 5000
};`}
  language="typescript"
/>

### 带行号和高亮的代码块

<CodeBlock
  showLineNumbers={true}
  highlightLines={[2, 3]}
  code={`function calculate(a: number, b: number) {
  const sum = a + b;
  const product = a * b;
  return { sum, product };
}`}
  language="typescript"
/>

## Tabs 组件测试

<Tabs defaultTab="basic">
  <TabPanel id="basic" label="基础用法">
    <p>这是基础用法的内容。</p>
    <CodeBlock
      code={`import { Button } from '@proton-ui/core';

<Button>点击我</Button>`}
      language="tsx"
    />
  </TabPanel>
  <TabPanel id="advanced" label="高级用法">
    <p>这是高级用法的内容。</p>
    <CodeBlock
      code={`import { Button } from '@proton-ui/core';

<Button
  variant="primary"
  size="large"
  onClick={() => console.log('clicked')}
>
  高级按钮
</Button>`}
      language="tsx"
    />
  </TabPanel>
  <TabPanel id="examples" label="示例">
    <p>这是示例内容。</p>
    <DemoBox code={`<Button>示例按钮</Button>`}>
      <button className="px-4 py-2 bg-green-500 text-white rounded">
        示例按钮
      </button>
    </DemoBox>
  </TabPanel>
</Tabs>

## Callout 组件测试

### Info 类型

<Callout type="info" title="提示信息">
  这是一条普通的提示信息，用于向用户传达一般性的信息。
</Callout>

### Warning 类型

<Callout type="warning" title="警告">
  这是一条警告信息，提醒用户注意某些可能的问题。
</Callout>

### Error 类型

<Callout type="error" title="错误">
  这是一条错误信息，表示某个操作失败或存在严重问题。
</Callout>

### Success 类型

<Callout type="success" title="成功">
  这是一条成功信息，表示操作已成功完成。
</Callout>

### Tip 类型

<Callout type="tip" title="小技巧">
  这是一条技巧提示，为用户提供有用的建议或最佳实践。
</Callout>

## 组合使用测试

<Tabs defaultTab="demo">
  <TabPanel id="demo" label="演示">
    <DemoBox
      title="组合演示"
      description="展示多个组件的组合使用"
      code={`<Button variant="primary">提交</Button>`}
    >
      <button className="px-6 py-3 bg-blue-500 text-white rounded-lg hover:bg-blue-600">
        提交
      </button>
    </DemoBox>
    <Callout type="tip" title="提示">
      你可以通过调整属性来改变按钮的外观和行为。
    </Callout>
  </TabPanel>
  <TabPanel id="api" label="API">
    <PropsTable
      data={[
        {
          name: 'children',
          type: 'ReactNode',
          required: true,
          description: '按钮内容'
        },
        {
          name: 'variant',
          type: "'primary' | 'secondary'",
          required: false,
          defaultValue: "'primary'",
          description: '按钮变体'
        }
      ]}
    />
  </TabPanel>
  <TabPanel id="code" label="代码">
    <CodeBlock
      filename="Button.tsx"
      showLineNumbers={true}
      code={`import React from 'react';

export const Button = ({ children, variant = 'primary' }) => {
  return (
    <button className={\`btn btn-\${variant}\`}>
      {children}
    </button>
  );
};`}
      language="tsx"
    />
  </TabPanel>
</Tabs>

## 测试总结

如果你能看到上面所有组件都正常渲染，说明文档组件系统已经成功配置！
