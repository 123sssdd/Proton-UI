# 快速开始

欢迎使用 Proton UI！一个专为 AI 对话场景设计的现代化 React 组件库,拥有独特的现代风美学和强大的流式渲染能力。

> 🚧 **维护中警告**：本组件库目前处于高度活跃的迭代维护中，API 可能会有 Breaking Changes，暂未发布至 NPM，仅供尝鲜和学习使用！

<QuickStartHighlights />

## 安装

### 方式 1: 完整安装 (推荐)

最简单的方式，直接安装 `core` 包，包含所有功能：

```bash
pnpm add @proton-ui/core @proton-ui/streaming
```

### 方式 2: 按需安装 (Headless / Modular)

如果你只需要特定的组件或逻辑，可以使用细粒度的包：

```bash
# 仅安装组件库
pnpm add @proton-ui/components @proton-ui/streaming

# 仅安装 Hooks (纯逻辑)
pnpm add @proton-ui/hooks
```

## 配置 Tailwind CSS

Proton UI 基于 Tailwind CSS 构建,你需要在项目中配置 Tailwind CSS。

### 1. 安装 Tailwind CSS

```bash
pnpm add -D tailwindcss postcss autoprefixer
npx tailwindcss init -p
```

### 2. 配置 tailwind.config.js

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
    // 添加 Proton UI 组件路径
    "./node_modules/@proton-ui/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

### 3. 导入样式

在你的入口文件中导入 Proton UI 的样式:

```tsx
// main.tsx 或 App.tsx
import "@proton-ui/core/dist/index.css";
```

## 快速使用

### 1. 特效系统

使用粒子效果和图片像素化,打造独特的视觉体验:

```tsx
import { ParticleEffect, PixelatedImage } from "@proton-ui/core";

function App() {
  return (
    <div className="relative">
      {/* 粒子效果 */}
      <ParticleEffect type="sparkle" count={20} autoTrigger />

      {/* 像素化图片 */}
      <PixelatedImage
        src="/avatar.jpg"
        alt="Avatar"
        pixelSize={8}
        colorLevels={16}
        className="rounded-lg"
      />
    </div>
  );
}
```

[探索更多特效 →](/components/effects/pixel-effects)

### 2. 主题系统

5 个精美主题,支持自定义配色:

```tsx
import { ThemeProvider } from "@proton-ui/core";

const customTheme = {
  primary: "#4ECDC4", // 霓虹青
  secondary: "#FF6B9D", // 樱粉
  accent: "#FFB86C", // 琥珀色
};

function App() {
  return (
    <ThemeProvider theme={customTheme}>
      <YourApp />
    </ThemeProvider>
  );
}
```

[查看主题画廊 →](/components/theme/theme-gallery)

### 3. 流式渲染

AI 对话场景的核心技术,逐字显示文本:

```tsx
import { Message } from "@proton-ui/core";
import { StreamingText } from "@proton-ui/streaming";

function ChatMessage({ content }: { content: string }) {
  return (
    <Message
      role="assistant"
      content={content}
      streaming
      renderContent={(text) => (
        <StreamingText
          content={text}
          batchSize={10}
          maxFPS={60}
          enableVirtualCompletion
        />
      )}
      timestamp={new Date()}
    />
  );
}
```

[了解流式渲染原理 →](/guide/streaming)

## 完整示例

这是一个完整的 AI 对话应用示例:

```tsx
import { ChatContainer, MessageInput } from "@proton-ui/core";
import { StreamingText } from "@proton-ui/streaming";
import { useState } from "react";

function ChatApp() {
  const [messages, setMessages] = useState([
    {
      id: "1",
      role: "assistant" as const,
      content: "你好！我是 AI 助手,有什么可以帮助你的吗？",
      timestamp: new Date(),
    },
  ]);
  const [loading, setLoading] = useState(false);

  const handleSend = async (content: string) => {
    // 添加用户消息
    const userMessage = {
      id: Date.now().toString(),
      role: "user" as const,
      content,
      timestamp: new Date(),
    };
    setMessages((prev) => [...prev, userMessage]);

    // 模拟 AI 流式响应
    setLoading(true);
    const aiResponse = "这是 AI 的回复内容...";

    // 创建流式消息
    const aiMessageId = (Date.now() + 1).toString();
    setMessages((prev) => [
      ...prev,
      {
        id: aiMessageId,
        role: "assistant" as const,
        content: "",
        streaming: true,
        renderContent: (text: string) => (
          <StreamingText content={text} enableVirtualCompletion />
        ),
        timestamp: new Date(),
      },
    ]);

    // 模拟流式输出
    let index = 0;
    const timer = setInterval(() => {
      if (index < aiResponse.length) {
        const newContent = aiResponse.slice(0, index + 1);
        setMessages((prev) =>
          prev.map((msg) =>
            msg.id === aiMessageId ? { ...msg, content: newContent } : msg
          )
        );
        index++;
      } else {
        clearInterval(timer);
        setLoading(false);
        setMessages((prev) =>
          prev.map((msg) =>
            msg.id === aiMessageId
              ? { ...msg, streaming: false, renderContent: undefined }
              : msg
          )
        );
      }
    }, 30);
  };

  return (
    <div className="h-screen flex flex-col">
      <div className="flex-1 overflow-hidden">
        <ChatContainer messages={messages} loading={loading} />
      </div>
      <MessageInput onSend={handleSend} disabled={loading} />
    </div>
  );
}

export default ChatApp;
```

### 可交互演示

试试这个完整的对话演示，体验所有功能：

<DemoBox
  title="完整对话演示"
  description="集成 ChatContainer、Message、MessageInput 和 StreamingText 的完整示例"
  code={`import { ChatContainer, MessageInput } from "@proton-ui/core";
import { StreamingText } from "@proton-ui/streaming";
import { useState } from "react";

export default () => {
  return <FullChatDemo />;
};`}
>
  <FullChatDemo />
</DemoBox>

## TypeScript 支持

Proton UI 完全使用 TypeScript 编写,提供完整的类型定义:

```tsx
import type { ButtonProps, MessageData } from "@proton-ui/core";
import type { StreamingTextProps } from "@proton-ui/streaming";

// 使用类型定义
const messages: MessageData[] = [
  {
    id: "1",
    role: "user",
    content: "Hello",
    timestamp: new Date(),
  },
];
```

## 探索更多

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-8">
  <a href="/components/effects/pixel-effects" className="block p-6 rounded-lg border-2 border-cyan-500 hover:shadow-lg transition-all">
    <div className="text-3xl mb-2">✨</div>
    <h3 className="text-lg font-bold mb-2">特效画廊</h3>
    <p className="text-sm text-gray-600 dark:text-gray-400">
      探索粒子效果和图片像素化的完整功能
    </p>
  </a>

<a
  href="/components/theme/theme-gallery"
  className="block p-6 rounded-lg border-2 border-pink-500 hover:shadow-lg transition-all"
>
  <div className="text-3xl mb-2">🎨</div>
  <h3 className="text-lg font-bold mb-2">主题画廊</h3>
  <p className="text-sm text-gray-600 dark:text-gray-400">
    查看 5 个精美主题和自定义主题编辑器
  </p>
</a>

<a
  href="/components/chat/chat-container"
  className="block p-6 rounded-lg border-2 border-purple-500 hover:shadow-lg transition-all"
>
  <div className="text-3xl mb-2">💬</div>
  <h3 className="text-lg font-bold mb-2">对话组件</h3>
  <p className="text-sm text-gray-600 dark:text-gray-400">
    了解 Message、ChatContainer 等对话组件
  </p>
</a>

  <a href="/components/button" className="block p-6 rounded-lg border-2 border-amber-500 hover:shadow-lg transition-all">
    <div className="text-3xl mb-2">💎</div>
    <h3 className="text-lg font-bold mb-2">基础组件</h3>
    <p className="text-sm text-gray-600 dark:text-gray-400">
      Button、Input、Card 等基础组件文档
    </p>
  </a>
</div>

## 常见问题

### 样式不生效?

确保你已经:

1. 正确配置了 Tailwind CSS
2. 在 `tailwind.config.js` 中添加了 Proton UI 的组件路径
3. 导入了 Proton UI 的样式文件

### 如何自定义组件样式?

所有组件都支持 `className` 属性:

```tsx
<Button className="bg-green-600 hover:bg-green-700">自定义按钮</Button>
```

### 浏览器兼容性?

Proton UI 支持所有现代浏览器:

- Chrome >= 90
- Firefox >= 88
- Safari >= 14
- Edge >= 90

## 获取帮助

- [GitHub Issues](https://github.com/123sssdd/Proton-UI/issues)
- [架构设计](/guide/architecture)
- [流式渲染原理](/guide/streaming)
