# Proton UI - AI 原生 React 组件库

> 针对 AI 对话与智能代理场景构建的深度工程化组件库

[![CI](https://github.com/123sssdd/Proton-UI/workflows/CI/badge.svg)](https://github.com/123sssdd/Proton-UI/actions)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![pnpm](https://img.shields.io/badge/maintained%20with-pnpm-cc00ff.svg)](https://pnpm.io/)

## 📖 项目简介

针对 AI 对话与智能代理（Agent）场景构建的深度工程化组件库。通过 Headless UI 抽象与 Rust 驱动的底层工具链，全链路解决 AI 产品在流式交互响应、超大异构数据传输等业务痛点，并攻克了大型 Monorepo 架构下的跨包协作冲突与文档构建性能瓶颈。

## ✨ 核心特性

### 🚀 高性能工具链与架构演进

- **Rust 驱动的构建系统**: 基于 Rspress（Rsbuild）实现毫秒级热启动与构建
- **极速打包**: 使用 tsup 产出 ESM/CJS 双格式并自动打包类型定义
- **并行化任务编排**: Turborepo 实现增量构建，整体构建效率提升约 5 倍

### 🏗️ Monorepo 研发链路深度治理

- **pnpm Workspaces**: 高效的磁盘空间利用和依赖管理
- **HMR 优化**: 解决 Vite 在 Workspace 环境下的 HMR 失效问题
- **跨包开发体验**: 配置 `optimizeDeps.exclude` 实现"修改即热更"

### ⚡️ 流式交互性能与状态机治理

- **增量渲染方案**: React.memo 缓存历史消息 + RAF 渲染节流
- **SSE 状态机**: 深度管控 Token 接收流
- **虚拟补全算法**: 解决 Markdown 标签未闭合导致的解析跳变
- **极致性能**: 确保流式输出过程中的视觉稳定性

### 📦 科研大数据传输方案

- **Web Worker 零拷贝**: 使用 Transferable Objects 实现高效通信
- **MD5 抽样哈希**: 特征值提取耗时从 10s+ 降至 200ms 以内
- **并发调度器**: 带优先级的并发控制与断点续传
- **IndexedDB 状态机**: 精细化状态管理

### 🛡️ 架构边界守卫与依赖一致性

- **eslint-plugin-boundaries**: 强制执行架构分层
- **pnpm Catalog**: 全局依赖索引，单一版本策略
- **循环依赖检测**: 杜绝跨包协作中的版本碎片化

### 🔄 规范化提交流水线与自动化发布

- **Husky + CommitLint**: Git hooks 自动化检查
- **cz-git**: 交互式规范提交，动态 Scope 检索
- **Changesets**: 自动计算版本、生成 CHANGELOG
- **版本联动**: 多包环境下的发布严谨性保证

### 🎨 Headless 与 AI 协议标准化

- **Headless UI 模式**: 逻辑与样式解耦
- **Tailwind CSS**: 原子化 Token 的语义一致性
- **AI 友好**: 为 AI 辅助编程提供高确定性的样式描述，代码生成准确度提升约 40%

### ✅ 自动化测试与视觉质量工程

- **Vitest**: 单元测试体系，确保 Headless Hooks 逻辑覆盖率
- **Playwright**: 视觉回归测试，像素级快照对比
- **增量测试**: Turborepo 缓存使 CI 流程耗时缩短 70%

## 🛠️ 技术栈

### 核心框架
- **React 18**: Concurrent Features 支持
- **TypeScript 5**: 完整的类型安全
- **Tailwind CSS 3**: Utility-First 样式方案

### 构建工具
- **Turborepo**: Monorepo 任务编排与缓存
- **tsup**: 基于 esbuild 的极速打包
- **Vite**: 开发服务器与 HMR
- **Rspress**: 基于 Rspack 的文档生成

### 包管理
- **pnpm 8**: 高效的依赖管理
- **Changesets**: 版本管理与 CHANGELOG

### 测试框架
- **Vitest**: 单元测试与属性测试
- **Playwright**: E2E 测试与视觉回归
- **fast-check**: 属性测试库

### 代码质量
- **ESLint**: 代码质量检查
- **Prettier**: 代码格式化
- **Husky**: Git hooks 管理
- **commitlint + cz-git**: 提交信息规范

## 📦 包结构

```
proton-ui/
├── packages/
│   ├── core/              # 核心 UI 组件
│   ├── streaming/         # 流式渲染引擎
│   └── docs/              # 文档站点
├── apps/
│   └── playground/        # 开发调试环境
├── configs/
│   ├── eslint-config/     # ESLint 共享配置
│   ├── typescript-config/ # TypeScript 共享配置
│   └── tailwind-config/   # Tailwind 共享配置
└── scripts/               # 构建脚本
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0

### 安装依赖

```bash
# 克隆仓库
git clone https://github.com/123sssdd/Proton-UI.git
cd Proton-UI

# 安装依赖
pnpm install
```

### 开发

```bash
# 启动开发服务器
pnpm dev

# 构建所有包
pnpm build

# 运行测试
pnpm test

# 代码检查
pnpm lint

# 类型检查
pnpm type-check
```

### 提交代码

```bash
# 使用交互式提交工具（推荐）
pnpm commit

# 或使用传统方式
git commit -m "feat(core): 新增 Button 组件"
```


## 🎯 核心亮点

### 1. 高性能流式渲染

```typescript
// 批处理 + RAF 节流
const renderer = new StreamingRenderer({
  batchSize: 10,
  maxFPS: 60,
});

renderer.append(chunk);
// 自动批量渲染，避免频繁 DOM 更新
```

### 2. 动态 Scope 检索

```bash
# 自动扫描 Monorepo 包结构
pnpm commit
# → 自动识别 packages/core、configs/eslint-config 等作为 scope
```

### 3. 增量构建

```bash
# 只构建变更的包
pnpm build
# → Turborepo 缓存命中率 > 80%
```

## 🤝 贡献指南

我们欢迎所有形式的贡献！

### 提交规范

使用 Conventional Commits 规范：

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

**类型**:
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具变更

**范围**: `core`, `streaming`, `docs`, `config`, `ci` 等

### 开发流程

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feat/amazing-feature`)
3. 提交变更 (`pnpm commit`)
4. 推送到分支 (`git push origin feat/amazing-feature`)
5. 创建 Pull Request

## 📄 许可证

[MIT](LICENSE) © 2024

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者！

## 📮 联系方式

- Issue: [GitHub Issues](https://github.com/123sssdd/Proton-UI/issues)
- Email: your-email@example.com

---

**Built with ❤️ for AI-Native Applications**
